/// Current environment of hashira
pub const HASHIRA_ENV: &str = "HASHIRA_ENV";

/// The target directory for the application.
pub const HASHIRA_TARGET_DIR: &str = "HASHIRA_TARGET_DIR";

/// The directory where the static files will be serve,
/// this directory exists within the `target_dir` and the wasm bundle is located.
pub const HASHIRA_PUBLIC_DIR: &str = "HASHIRA_PUBLIC_DIR";